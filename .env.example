# =============================================================================
# LyCore Environment Configuration - Example
# =============================================================================
# This setup keeps the built-in website audit form.
# The form submits to /api/leads, then the server forwards the lead to Zoho.
# Do not put private keys in VITE_* variables.
# =============================================================================

# Public Cal.com booking link shown on /book.
VITE_BOOKING_URL="https://cal.com/your-username/your-event"

# Google Analytics 4 measurement ID.
# Find it in GA4 Admin > Data streams > Web > Measurement ID. Format: G-XXXXXXXXXX.
VITE_GA4_MEASUREMENT_ID="G-XXXXXXXXXX"

# Zoho Flow webhook URL for website audit form submissions.
# Create this in Zoho Flow with a Webhook trigger, then map the payload to Zoho CRM Lead fields.
ZOHO_CRM_WEBHOOK_URL=""

# Optional generic CRM webhook fallback. Leave blank if using ZOHO_CRM_WEBHOOK_URL.
CRM_WEBHOOK_URL=""

# Gemini API key used by the website AI chat endpoint.
# Keep this server-side only. Do not prefix it with VITE_.
GEMINI_API_KEY=""

# Use these public URLs inside your form/calendar platform redirect settings:
# Intake form success redirect: https://lycore.org/audit-request-received
# Calendar booking success redirect: https://lycore.org/booking-confirmed
# Calendar booking failed/canceled redirect: https://lycore.org/booking-failed

# Public production URL.
APP_URL="https://lycore.org"

# Local development port.
PORT="3000"
