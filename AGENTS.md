# Repository workflow

After completing and verifying any change in this repository, stage the relevant changes, create a clear commit directly on `main`, and push `main` to the configured `origin` remote.

If a push cannot be completed because authentication, permissions, network access, or another external requirement is unavailable, report the exact blocker and leave the verified changes ready to push. Do not silently skip the push.
